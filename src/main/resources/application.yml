spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/vollmed-consultorio
    username: postgres
    password: 1234

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true